import React from "react"
import { useState } from "react"

export default function TransitionLoading() {

	return (
		<>
			<div className="fixed w-screen h-screen top-0 left-0 z-50 bg-black opacity-80 flex justify-center font-pop font-bold items-center text-white">
			</div>
		</>
	)
}